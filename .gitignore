# ── Generated experiment artifacts (regenerated by the pipeline) ────────────
latest/
results/
vocab_output/
processed/
wandb/
logs/
gt_cache/
.audit_vocab/

# NOTE: analysis/ is SOURCE CODE (plotting, tables, probe, run_analysis) and is
# intentionally NOT ignored. If you want to ignore only the generated plots,
# they live under results/plots/ which is already covered by results/ above.

# ── Local artifact validation / report outputs (never commit) ───────────────
_validate_vocab/
_validation_results/

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/

# ── Model / data blobs that shouldn't be in git ─────────────────────────────
*.pt
*.pth
*.pickle
*.ckpt
# Whole data folder (datasets are downloaded/regenerated, not versioned).
data/

# ── Design retrospective (report/deck + exported source chats; not for GitHub) ─
reports/

# ── OS / editor ─────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

analysis/build_vocab_report.py
analysis/vocab_report.html

sweep/
# Agent instruction file — local only, not part of the repository
CLAUDE.md

# Generated result artifacts (LaTeX tables + CSVs) — kept ON DISK, never tracked.
# These are outputs of build_metric_set.py / make_acm_tables.py /
# build_ablation_set.py / make_ablation_tables.py; they declutter code review and
# are never committed. (A second copy lives OUTSIDE the repo at
# ../results_offline as a safety backup — see note.)
results_offline/
ablation_results/
results_v1/
thesis_results/
tables_main/
tables_v*/
offline_results/

*.md